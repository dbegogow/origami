import menuStyle from '../Menu.module.css';

const ContactUs = () => {
    return (
        <main className={menuStyle.main}>
            <h1 className={menuStyle.heading}>Contact Us Page</h1>
        </main>
    );
};

export default ContactUs;