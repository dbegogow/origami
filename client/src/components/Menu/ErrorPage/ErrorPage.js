import menuStyle from '../Menu.module.css';

const ErrorPage = () => {
    return (
        <main className={menuStyle.main}>
            <h1 className={menuStyle.heading}>Error</h1>
        </main>
    );
};

export default ErrorPage;