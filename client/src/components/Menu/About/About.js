import menuStyle from '../Menu.module.css';

const About = () => {
    return (
        <main className={menuStyle.main}>
            <h1 className={menuStyle.heading}>About Page</h1>
        </main>
    );
};

export default About;